<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PriceTracker.PriceScannerOCR"
             x:Class="PriceTracker.MainPage">

    <CollectionView ItemsSource="{Binding data}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Grid Margin="0,0,0,4" RowSpacing="2" ColumnSpacing="10" Padding="10" BackgroundColor="AliceBlue">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="100" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Label Grid.Column="0" TextColor="#000000" Text="{Binding Name}" FontAttributes="Bold" />
                    <CollectionView Grid.Column="1" ItemsSource="{Binding Title}" />
                    <CollectionView Grid.Column="2" ItemsSource="{Binding Price}" />
                </Grid>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>

</ContentPage>
